# Required: Anthropic API key used by the Claude Agent SDK.
ANTHROPIC_API_KEY=

# Required: absolute path to the built payments-toolkit-mcp entry point
# (run `pnpm run build` in that repo first). Example:
# MCP_SERVER_PATH=/Users/you/dev/payments-toolkit-mcp/dist/index.js
MCP_SERVER_PATH=
